set backspace=2            " makes backspace in insert mode behave naturally
syntax on                  " syntax highlighting
filetype indent on         " filetype-aware indenting
set autoindent             " preserves indentation
set number                 " show line numbers
colorscheme evening        " built-in colorscheme
set nobackup               " no backup files (~)
set ignorecase             " search ignoring case
set smartcase              " case-sensitive if search contains uppercase
set hlsearch              " highlight all search matches
set incsearch             " highlight as you type the search
